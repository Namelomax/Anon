# Зависимости анонимайзера (серверная часть).
# Установка:  pip install -r requirements.txt
# (на JupyterHub без прав — pip сам поставит в --user)

# --- Обработка документов -------------------------------------------------
python-docx        # .docx — чтение/запись с сохранением структуры
openpyxl           # .xlsx / .xlsm
xlrd>=2.0.1        # .xls  — старый Excel 97-2003
olefile            # .doc  — старый Word 97-2003 (нативный чистый-Python разбор)
pypdf              # .pdf  — извлечение текстового слоя
lxml               # .xml, .odt

# --- NER / модели ---------------------------------------------------------
gliner             # основной NER-детектор (тянет transformers; torch — ниже)
transformers       # бэкенд для GLiNER
huggingface-hub    # загрузка чекпойнтов моделей
# torch — ставить ПОД СВОЮ платформу отдельно (важно для CUDA):
#   CUDA:  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU:   pip install torch
# natasha          # альтернативный лёгкий NER-бэкенд (опционально, --ner natasha)

# --- Клиент / прочее (опционально, серверу не нужно) ----------------------
# streamlit        # тонкий UI-клиент
# torch-directml   # GPU на Windows через DirectML

# --- .doc (Word 97-2003) --------------------------------------------------
# Обрабатывается НАТИВНО через olefile (выше) — системные утилиты НЕ нужны.
# Если на сервере всё же есть antiword / catdoc / LibreOffice, documents.py
# сперва использует их (чуть выше точность), иначе — чистый Python-разбор.
