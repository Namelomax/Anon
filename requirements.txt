# --- Anonymizer dependencies ---
# Core (always needed):
gliner>=0.2          # zero-shot multilingual NER (PERSON/LOCATION/ORG)
python-docx>=1.0     # read/write .docx (also pulls lxml, used for .xml/.odt)
pymorphy2>=0.9       # Russian morphological dictionary for the PERSON un-mask
pymorphy2-dicts-ru   # gate in detectors.py (rare surnames must never be
                     # un-maskable by the review model)

# Document formats (text extraction for input):
pypdf>=4.0           # .pdf
openpyxl>=3.1        # .xlsx / .xlsm
# .rtf and .odt are read with the stdlib + lxml (no extra dependency).

# Optional:
natasha>=1.6         # only for the --natasha NER backend (CPU, numpy); also
                     # a fallback dictionary source for the PERSON un-mask gate
streamlit>=1.30      # only for the web UI (anonymizer/app.py)

# PyTorch is required by GLiNER but install it to match your hardware/CUDA:
#   CPU:   pip install torch
#   CUDA:  pip install torch --index-url https://download.pytorch.org/whl/cu124
# (On the GPU host it is already installed — do NOT reinstall and downgrade it.)
